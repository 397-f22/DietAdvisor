const NutritionInfo = ({nutrients}) => {
    return (
        <div>
            {
                Object.entries(nutrients).map(()=>{})
            }
        </div>
    )
}

export default NutritionInfo;